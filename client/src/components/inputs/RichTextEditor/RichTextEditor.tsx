import { SerializedSlateContent } from '@libs';
import { ContextProvider, Editable } from './components';



export const RichTextEditor = {
    ContextProvider,
    Editable,
    Serialized: SerializedSlateContent,
};